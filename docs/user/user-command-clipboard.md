
Clipboard for use to keep useful and commonly resued snippets to limit having to regenerate said items




#check on install status (in container)
ps aux | grep install
tail -f /var/log/ac-setup.log

#install progress
ps aux | grep install
cat /tmp/install.sh | head -20
